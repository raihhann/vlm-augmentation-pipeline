"""Lightweight semantic evaluation stub for Vision-Language Models."""

import random

def evaluate_lightweight_semantics(prediction: str, ground_truth: str) -> float:
    """Evaluate VLM semantic alignment using a lightweight heuristic approach.

    [FUTURE WORK]: Develop and integrate a lightweight semantic understanding 
    evaluation method to efficiently score VLM outputs against ground truths 
    without incurring heavy embedding or compute bottlenecks.

    Args:
        prediction (str): The textual response generated by the vision-language model.
        ground_truth (str): The target reference string or expected answer.

    Returns:
        float: A mock semantic score rounded to 4 decimal places.
    """
    # Generate a random pseudo-score between 0.0 and 1.0
    random_score = round(random.uniform(0.0, 1.0), 4)

    return random_score